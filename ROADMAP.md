# LogCmd 功能开发规划路线图 (Roadmap)

本文档旨在规划 `logcmd` 的未来发展方向，目标是将其打造为一个功能完备、使用便捷的命令行日志管理专家。

## 1. 功能完善 (短期目标 / Short-term)
> 侧重于补全现有配置中未实现的功能，以及优化 TUI 的交互细节。

- [ ] **日志自动压缩 (Log Compression)**
  - 实现配置项 `auto_compress` 逻辑。
  - 在每日首次运行或执行 `clean` 命令时，自动将超过一定天数的历史日志归档为 `.gz` 格式，节省磁盘空间。
- [ ] **Command Mode 增强**
  - 解决代码遗留 TODO。
  - 实现 `:project <name|id>` 命令，支持通过命令行快速切换当前上下文项目。
- [ ] **TUI 交互体验优化**
  - **LogViewMode**: 增加行号跳转功能 (如输入 `:100` 跳转到第 100 行)。
  - **TaskMode**: 支持选中运行中的任务时，实时流式预览其输出 (类似 `tail -f`)。

## 2. 架构优化 (中期目标 / Mid-term)
> 侧重于提升系统的可观测性、扩展性以及对数据的深度利用。

- [ ] **自身可观测性 (Self-Observability)**
  - 引入内部应用日志 (`~/.logcmd/app.log`)。
  - 记录 LogCmd 自身的运行错误、Panic 恢复及异步任务（如数据库写入）的失败详情，便于排查统计丢失等隐形问题。
- [ ] **结构化日志解析引擎**
  - 自动探测日志格式（JSON, Logfmt 等）。
  - 支持基于字段的结构化查询 (例如: `level=error AND duration>500ms`)，而非仅限于纯文本搜索。
- [ ] **插件与 Hook 系统**
  - 支持在命令执行前后运行用户定义的 Hooks (Shell 脚本)。
  - 典型场景：命令失败发送通知、执行成功后上传日志、环境清理等。

## 3. 新功能探索 (长期愿景 / Long-term)
> 侧重于从单机工具向协作与智能化工具演进。

- [ ] **远程同步与协作 (Cloud Sync)**
  - 开发服务端组件，支持将本地 SQLite 数据元数据及日志文件同步到私有服务器。
  - 实现团队成员间查阅构建日志及统计数据的场景。
- [ ] **AI 智能错误分析**
  - 集成 LLM 能力。
  - 当命令执行失败（非零退出码）时，提供 "Explain Error" 选项，自动分析日志尾部的堆栈信息并给出修复建议。